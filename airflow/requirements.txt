# duckdb is pinned explicitly: unpinned, dbt-duckdb resolves it to 1.5.x, which
# writes a warehouse the Superset image cannot read. Keep this in step with
# superset/requirements.txt.
dbt-core==1.10.15
dbt-duckdb==1.10.0
duckdb==1.4.4
